{
  "name": "peripartner-backend",
  "version": "1.0.0",
  "description": "Peripartner: webhook → watermark PDF → one-time download link",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pdf-lib": "^1.17.1",
    "stripe": "^20.4.0"
  }
}
